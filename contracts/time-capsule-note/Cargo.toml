[package]
name = "time-capsule-note"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
miden = { version = "0.10" }

[package.metadata.component]
package = "miden:time-capsule-note"

[package.metadata.miden]
project-kind = "note-script"
